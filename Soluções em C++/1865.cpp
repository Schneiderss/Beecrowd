#include <bits/stdc++.h>
using namespace std;

int main(){

    int n, k;
    string s;
    cin >> n;
    while(n--){
        cin >> s >> k;

        cout << ((s == "Thor") ? "Y" : "N") << endl;

    }

    return 0;
}